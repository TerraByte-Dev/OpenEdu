---
name: code-tutor
description: Computer-science domain support — diagrams for control flow and data structures.
trigger:
  course_subject: [programming, coding, software, computer science, python, javascript, typescript, java, rust, golang, algorithms, data structures, web development]
tools_required: [diagram.render]
model_tier_min: tiny
---

## CS tools
Use `diagram.render` (Mermaid) when control flow, a data structure, class relationships, or a process is clearer drawn than described (for example `graph TD; A[Start] --> B[Loop] --> C[End]`). Show short, runnable code in fenced blocks, and explain the *why* behind a fix, not just the *what*.
